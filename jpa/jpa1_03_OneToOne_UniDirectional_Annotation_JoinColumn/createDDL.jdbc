CREATE TABLE CUSTOMER (ID INTEGER NOT NULL, NAME VARCHAR(255), MY_ORDER_FK INTEGER, PRIMARY KEY (ID))
CREATE TABLE ORDER_TABLE (ORDER_ID INTEGER NOT NULL, SHIPPING_ADDRESS VARCHAR(255), PRIMARY KEY (ORDER_ID))
ALTER TABLE CUSTOMER ADD CONSTRAINT CUSTOMERMYORDER_FK FOREIGN KEY (MY_ORDER_FK) REFERENCES ORDER_TABLE (ORDER_ID)
