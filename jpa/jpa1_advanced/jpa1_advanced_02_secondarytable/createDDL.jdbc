CREATE TABLE my_own_employee_table (ID INTEGER NOT NULL, my_name VARCHAR(255), PRIMARY KEY (ID))
CREATE TABLE atable (ID INTEGER NOT NULL, PRIMARY KEY (ID))
CREATE TABLE salary (ID INTEGER NOT NULL, my_salary BIGINT, PRIMARY KEY (ID))
ALTER TABLE atable ADD CONSTRAINT FK_atable_ID FOREIGN KEY (ID) REFERENCES my_own_employee_table (ID)
ALTER TABLE salary ADD CONSTRAINT FK_salary_ID FOREIGN KEY (ID) REFERENCES my_own_employee_table (ID)
