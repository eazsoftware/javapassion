CREATE TABLE STUDENT2 (ID BIGINT NOT NULL, SCHOOL VARCHAR(255), GRADE FLOAT, PRIMARY KEY (ID))
CREATE TABLE PERSON2 (ID BIGINT NOT NULL, DTYPE VARCHAR(31), NAME VARCHAR(255), PRIMARY KEY (ID))
ALTER TABLE STUDENT2 ADD CONSTRAINT FK_STUDENT2_ID FOREIGN KEY (ID) REFERENCES PERSON2 (ID)
CREATE TABLE SEQUENCE (SEQ_NAME VARCHAR(50) NOT NULL, SEQ_COUNT DECIMAL, PRIMARY KEY (SEQ_NAME))
INSERT INTO SEQUENCE(SEQ_NAME, SEQ_COUNT) values ('SEQ_GEN', 1)
