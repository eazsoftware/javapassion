CREATE TABLE CUSTOMER (ID INTEGER NOT NULL, NAME VARCHAR(255), ORDER_ORDER_ID INTEGER, PRIMARY KEY (ID))
CREATE TABLE ORDER_TABLE (ORDER_ID INTEGER NOT NULL, SHIPPING_ADDRESS VARCHAR(255), PRIMARY KEY (ORDER_ID))
ALTER TABLE CUSTOMER ADD CONSTRAINT CSTOMERRDERORDERID FOREIGN KEY (ORDER_ORDER_ID) REFERENCES ORDER_TABLE (ORDER_ID)
