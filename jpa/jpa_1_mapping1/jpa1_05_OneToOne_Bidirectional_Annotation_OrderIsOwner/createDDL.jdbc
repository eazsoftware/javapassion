CREATE TABLE CUSTOMER (ID INTEGER NOT NULL, NAME VARCHAR(255), PRIMARY KEY (ID))
CREATE TABLE ORDER_TABLE (ORDER_ID INTEGER NOT NULL, SHIPPING_ADDRESS VARCHAR(255), MY_CUSTOMER_ID_FK INTEGER, PRIMARY KEY (ORDER_ID))
ALTER TABLE ORDER_TABLE ADD CONSTRAINT RDRTBLEMYCSTMRIDFK FOREIGN KEY (MY_CUSTOMER_ID_FK) REFERENCES CUSTOMER (ID)
