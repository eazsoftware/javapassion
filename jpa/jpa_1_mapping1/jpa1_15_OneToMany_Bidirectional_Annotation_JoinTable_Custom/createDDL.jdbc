CREATE TABLE my_join_table (my_customer_id INTEGER NOT NULL, my_order_id INTEGER NOT NULL, PRIMARY KEY (my_customer_id, my_order_id))
CREATE TABLE CUSTOMER (ID INTEGER NOT NULL, NAME VARCHAR(255), PRIMARY KEY (ID))
CREATE TABLE ORDER_TABLE (ORDER_ID INTEGER NOT NULL, SHIPPING_ADDRESS VARCHAR(255), CUSTOMER_ID INTEGER, PRIMARY KEY (ORDER_ID))
ALTER TABLE my_join_table ADD CONSTRAINT myjintablemyrderid FOREIGN KEY (my_order_id) REFERENCES ORDER_TABLE (ORDER_ID)
ALTER TABLE my_join_table ADD CONSTRAINT myjntblemycstmerid FOREIGN KEY (my_customer_id) REFERENCES CUSTOMER (ID)
ALTER TABLE ORDER_TABLE ADD CONSTRAINT RDERTABLECSTOMERID FOREIGN KEY (CUSTOMER_ID) REFERENCES CUSTOMER (ID)
