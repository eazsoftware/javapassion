CREATE TABLE EVENT_SPEAKER (events_ID BIGINT NOT NULL, speakers_ID BIGINT NOT NULL, PRIMARY KEY (events_ID, speakers_ID))
CREATE TABLE SPEAKER (ID BIGINT GENERATED ALWAYS AS IDENTITY NOT NULL, FIRSTNAME VARCHAR(255), LASTNAME VARCHAR(255), PRIMARY KEY (ID))
CREATE TABLE EVENT (ID BIGINT GENERATED ALWAYS AS IDENTITY NOT NULL, STARTDATE DATE, DURATION INTEGER, NAME VARCHAR(255), PRIMARY KEY (ID))
ALTER TABLE EVENT_SPEAKER ADD CONSTRAINT VENTSPEAKERventsID FOREIGN KEY (events_ID) REFERENCES EVENT (ID)
ALTER TABLE EVENT_SPEAKER ADD CONSTRAINT VNTSPEAKERspkersID FOREIGN KEY (speakers_ID) REFERENCES SPEAKER (ID)
