CREATE TABLE SPEAKER (ID BIGINT GENERATED ALWAYS AS IDENTITY NOT NULL, FIRSTNAME VARCHAR(255), LASTNAME VARCHAR(255), PRIMARY KEY (ID))
CREATE TABLE SPEAKER_EVENT (events_ID BIGINT NOT NULL, speakers_ID BIGINT NOT NULL, PRIMARY KEY (events_ID, speakers_ID))
CREATE TABLE EVENT (ID BIGINT GENERATED ALWAYS AS IDENTITY NOT NULL, STARTDATE DATE, DURATION INTEGER, NAME VARCHAR(255), PRIMARY KEY (ID))
ALTER TABLE SPEAKER_EVENT ADD CONSTRAINT SPKEREVENTspkersID FOREIGN KEY (speakers_ID) REFERENCES SPEAKER (ID)
ALTER TABLE SPEAKER_EVENT ADD CONSTRAINT SPAKEREVENTventsID FOREIGN KEY (events_ID) REFERENCES EVENT (ID)
