CREATE TABLE my_join_table (my_event_id BIGINT NOT NULL, my_speaker_id BIGINT NOT NULL, PRIMARY KEY (my_event_id, my_speaker_id))
CREATE TABLE SPEAKER (ID BIGINT GENERATED ALWAYS AS IDENTITY NOT NULL, FIRSTNAME VARCHAR(255), LASTNAME VARCHAR(255), PRIMARY KEY (ID))
CREATE TABLE EVENT (ID BIGINT GENERATED ALWAYS AS IDENTITY NOT NULL, STARTDATE DATE, DURATION INTEGER, NAME VARCHAR(255), PRIMARY KEY (ID))
ALTER TABLE my_join_table ADD CONSTRAINT myjintablemyventid FOREIGN KEY (my_event_id) REFERENCES EVENT (ID)
ALTER TABLE my_join_table ADD CONSTRAINT myjntablemyspkerid FOREIGN KEY (my_speaker_id) REFERENCES SPEAKER (ID)
