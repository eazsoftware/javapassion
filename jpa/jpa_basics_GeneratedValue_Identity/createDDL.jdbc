CREATE TABLE STUDENT (ID INTEGER GENERATED ALWAYS AS IDENTITY NOT NULL, NAME VARCHAR(255), PRIMARY KEY (ID))
CREATE TABLE my_own_employee_table (ID INTEGER GENERATED ALWAYS AS IDENTITY NOT NULL, SALARY BIGINT, BONUS FLOAT, NAME VARCHAR(255), B SMALLINT DEFAULT 0, PRIMARY KEY (ID))
